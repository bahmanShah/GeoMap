<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GeoMap" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="fontFamily">@font/iran</item>
        <item name="android:navigationBarColor">@color/card</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/card</item>
    </style>

    <style name="circleImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
</resources>